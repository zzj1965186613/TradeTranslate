{
  "name": "tradetranslate",
  "version": "1.1.0",
  "private": true,
  "description": "Multilingual translation for WhatsApp Web with multiple AI provider support",
  "scripts": {
    "build": "vite build && node scripts/copy-static.mjs",
    "dev": "vite build --watch"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.270",
    "typescript": "^5.4.0",
    "vite": "^5.4.0"
  }
}
